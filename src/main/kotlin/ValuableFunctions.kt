class ValuableFunctions {
    /*
    * toList()
    * groupBy()
    * filter() and filterKeys() & filterValues() - filter() is used for filtering the elements of a collection and filterValues() is used for filtering the entries of a map based on the values.
    * map()
    * forEach() and forEachIndexed()
    * indexOf() and lastIndexOf()
    * maxByOrNull() and max()
    *
    * */
}